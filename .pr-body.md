## O que muda

- **Retry transitório** no `graphPost`: até 5 tentativas, 5s de espera, ativado **somente** por `is_transient:true` / code 2 / status ≥ 500.
- **Token morto (190) aborta na primeira tentativa** — nunca entra no retry (não existe retry que conserte token revogado, e não vamos mascarar um `190` como se fosse transitório).

## Por que agora
O job de 18/09 caiu no Threads com `500 {"is_transient":true}` — erro que a Meta marca como "retry later". Faltava o retry no script.

## Escopo decidido pelo Rilson
- **Facebook fora do padrão** (o workflow_dispatch default já é `instagram,threads` — nada a mudar no job).
- IG Threads token `190` = troca de senha → renovação manual no navegador (fora do escopo desta PR).
